include ping_all_pisces_fattree.ini

topology_name = fat_tree

topology {
  num_core_switches = 4
  down_ports_per_core_switch = 40
  num_agg_subtrees = 10
  num_agg_switches_per_subtree = 4
  up_ports_per_agg_switch = 4
  down_ports_per_agg_switch = 4 
  num_leaf_switches_per_subtree = 4
  up_ports_per_leaf_switch = 4
}

router = fat_tree

network_nodes_per_switch = 2
