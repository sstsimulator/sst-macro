#! /usr/bin/env python

import os
import commands
import sys

if os.path.isdir(".git"):
  sys.stdout.write("%s\n" % commands.getoutput("git rev-parse --abbrev-ref HEAD").strip())
else:
  sys.stdout.write("\n")

