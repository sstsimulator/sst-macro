# Configuration parameters for running a parsedumpi computation

node {
 app1 {
  indexing = block
  allocation = first_available
  name = mpi_coverage
  launch_cmd = aprun -n 24 -N 4
  start = 0ms
 }
}

